// Brand-moment surfaces ONLY (splash, share card, wordmark). Never functional UI.
// Kept out of the flat `colors` palette so it can't be reached for by accident.
export const brandGradientStart = '#2E5E43'; // == colors.primary
export const brandGradientEnd = '#66B287'; // == colors.greenLight
